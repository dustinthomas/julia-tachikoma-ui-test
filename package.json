{
  "name": "julia-tachikoma-ui-test",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'Legacy placeholder. Use: julia --project=.'",
    "build": "echo 'Not applicable (native Julia TUI)'",
    "test": "echo 'Use: julia --project=. -e \"using Pkg; Pkg.test()\"' && exit 0",
    "typecheck": "echo 'Not applicable (Julia)'"
  },
  "devDependencies": {}
}
